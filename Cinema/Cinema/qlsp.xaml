<Page x:Class="Cinema.qlsp"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:Cinema"
      mc:Ignorable="d"
      Title="Quản lý sản phẩm bắp nước - Cinema" Height="650" Width="1000" Background="#F0F2F5">

    <Page.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#57606F"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Foreground" Value="#2F3542"/>
            <Setter Property="CaretBrush" Value="#E5A93C"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="#DCDDE1" BorderThickness="1" Background="White" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E5A93C"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F2F6"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E1E4E8"/>
                                <Setter Property="Foreground" Value="#A4B0BE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="#E5A93C">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D4992C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="20" BorderBrush="#E1E4E8" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" ShadowDepth="2" Color="#D1D8E0" Opacity="0.3"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <TextBlock Text="🍿" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="THÔNG TIN SẢN PHẨM" FontSize="16" FontWeight="ExtraBold" Foreground="#2F3542" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="Mã sản phẩm:" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtId" IsReadOnly="True" Height="38" Margin="0,0,0,12"/>

                    <TextBlock Text="Tên mặt hàng:" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtName" Height="38" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Giá (VNĐ):" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="txtPrice" Height="38"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Tồn Kho:" Style="{StaticResource LabelStyle}" Foreground="#27AE60"/>
                            <TextBox x:Name="txtQuantity" Height="38" BorderBrush="#27AE60" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Phân loại:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="cmbCategory" Height="38" Margin="0,0,0,12" SelectedIndex="0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Bắp rang"/>
                        <ComboBoxItem Content="Nước ngọt"/>
                        <ComboBoxItem Content="Combo"/>
                        <ComboBoxItem Content="Snack khác"/>
                    </ComboBox>

                    <TextBlock Text="Trạng thái:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="cmbStatus" Height="38" Margin="0,0,0,20" SelectedIndex="0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Đang kinh doanh"/>
                        <ComboBoxItem Content="Ngừng kinh doanh"/>
                    </ComboBox>

                    <Rectangle Height="1" Fill="#F1F2F6" Margin="0,0,0,20"/>
                    <Button x:Name="btnAdd" Content="➕ THÊM SẢN PHẨM MỚI" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,10" Click="btnAdd_Click"/>
                    <UniformGrid Columns="2">
                        <Button x:Name="btnEdit" Content="✏️ Cập Nhật" Style="{StaticResource ActionButtonStyle}" Background="#3498DB" Margin="0,0,4,0" Click="btnEdit_Click"/>
                        <Button x:Name="btnDelete" Content="🗑️ Xóa Bỏ" Style="{StaticResource ActionButtonStyle}" Background="#E74C3C" Margin="4,0,0,0" Click="btnDelete_Click"/>
                    </UniformGrid>
                    <Button x:Name="btnClear" Content="🔄 Làm mới form" Style="{StaticResource ActionButtonStyle}" Background="#747D8C" Margin="0,10,0,0" Click="btnClear_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border Grid.Column="1" Margin="15,0,0,0" Background="White" CornerRadius="15" Padding="15" BorderBrush="#E1E4E8" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" ShadowDepth="2" Color="#D1D8E0" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="5,0,0,15">
                    <Rectangle Width="4" Height="20" Fill="#E5A93C" Margin="0,0,10,0" RadiusX="2" RadiusY="2"/>
                    <TextBlock Text="DANH SÁCH SẢN PHẨM TRONG KHO" FontSize="16" FontWeight="Bold" Foreground="#2F3542" VerticalAlignment="Center"/>
                </StackPanel>

                <DataGrid x:Name="dgProducts" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" 
                          SelectionMode="Single" SelectionChanged="dgProducts_SelectionChanged"
                          Background="Transparent" BorderThickness="0" RowHeight="45" FontSize="13" 
                          HeadersVisibility="Column" GridLinesVisibility="None" Foreground="#2F3542">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#57606F"/>
                            <Setter Property="Padding" Value="10,12"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#E5A93C"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F1F2F6"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FFF4E0"/>
                                    <Setter Property="Foreground" Value="#D4992C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã" Binding="{Binding ma_san_pham}" Width="Auto" MinWidth="50">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#A4B0BE"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Tên Sản Phẩm" Binding="{Binding ten_san_pham}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="10,0,0,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Giá Bán" Binding="{Binding gia_ban, StringFormat={}{0:N0}đ}" Width="Auto" MinWidth="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Số Lượng" Binding="{Binding so_luong_ton}" Width="Auto" MinWidth="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#27AE60"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Loại" Binding="{Binding loai}" Width="Auto" MinWidth="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Trạng Thái" Binding="{Binding trang_thai}" Width="Auto" MinWidth="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>